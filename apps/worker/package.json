{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run tests/api.test.ts"
  },
  "dependencies": {
    "@fincell/shared": "workspace:*",
    "hono": "^4.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240215.0",
    "tsx": "^4.7.1",
    "typescript": "^5.4.0",
    "vitest": "^1.3.1",
    "wrangler": "^3.30.0"
  }
}
